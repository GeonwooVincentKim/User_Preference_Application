<?xml version="1.0" encoding="utf-8"?>
<androidx.cardview.widget.CardView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/itemLayoutCardView"
    android:layout_width="match_parent"
    android:layout_height="180sp"
    android:layout_margin="10sp">

    <androidx.constraintlayout.widget.ConstraintLayout
        android:id="@+id/itemLayoutInnerCardView"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        app:layout_constraintLeft_toLeftOf="parent"
        app:layout_constraintRight_toRightOf="parent">

        <ImageView
            android:id="@+id/itemImage"
            android:layout_width="match_parent"
            android:layout_height="120sp"
            android:contentDescription="@string/stringToDo"
            android:src="@color/cardview_dark_background"
            app:layout_constraintLeft_toLeftOf="parent"
            app:layout_constraintRight_toRightOf="parent"
            app:layout_constraintTop_toTopOf="parent" />

        <TextView
            android:id="@+id/itemTitleText"
            android:layout_width="match_parent"
            android:layout_height="35sp"
            android:text="@string/stringTitleText_InformationPage"
            android:textAlignment="center"
            android:textColor="@color/black"
            android:textSize="28.8sp"
            android:textStyle="bold"
            app:layout_constraintTop_toBottomOf="@id/itemImage" />

        <TextView
            android:id="@+id/itemContextText"
            android:layout_width="match_parent"
            android:layout_height="25sp"
            android:text="@string/stringContextText"
            android:textAlignment="center"
            android:textSize="19.2sp"
            android:textStyle="bold"
            app:layout_constraintTop_toBottomOf="@id/itemTitleText" />

    </androidx.constraintlayout.widget.ConstraintLayout>
</androidx.cardview.widget.CardView>
